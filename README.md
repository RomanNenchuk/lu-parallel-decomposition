# LU Decomposition Project

Проєкт для обчислення LU-розкладу матриць із використанням різних підходів (послідовний та паралельний через OpenMP).

## 🛠 Компіляція

Для збірки використовується `Makefile`. Вам знадобиться компілятор `g++`.

### Linux / macOS
Виконайте команду в терміналі:
```bash
make
```

### Windows (MinGW / MSYS2)
Виконайте команду в командному рядку:
```cmd
mingw32-make
```

---

## 🚀 Запуск

Після збірки буде створено виконуваний файл `lu` (або `lu.exe` на Windows).

1. **(Опціонально) Кількість потоків:**
   * Linux/macOS: `export OMP_NUM_THREADS=4`
   * Windows (CMD): `set OMP_NUM_THREADS=4`

2. **Запуск програми:**
   ```bash
   ./lu
   ```

---

## 📂 Структура
* `src/` — вихідний код.
* `include/` — заголовні файли.
* `Makefile` — скрипт автоматизації збірки.